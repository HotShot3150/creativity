<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @project.title %>
</p>

<div id="project_image">
  <% @project.image.each do |photo| %>
    <div class="photo">
      <%= image_tag project.image_url.to_s %>
      <div class="name"><%= image.name %></div>
      <div class="actions">
        <%= link_to "edit", edit_image_path(image) %> |
        <%= link_to "remove", image, :confirm => 'Are you sure?', :method => :delete %>
      </div>
    </div>
  <% end %>
  <div class="clear"></div>
</div>

<p>
  <b>Body:</b>
  <%= @project.body %>
</p>

  <b>Category:</b>
  <%= @project.category_id = current %>
</p>


<% if can? :update, @project %>
  <%= link_to 'Edit', edit_project_path, class: "button radius" %>
<% end %>
<% if can? :destory, @prpject%>
  <%= link_to 'Delete', category_project_path, method: :delete, data: { confirm: 'Are you sure?' }, class: "button radius alert" %>
<% end %>
